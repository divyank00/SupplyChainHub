<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.smartcontract">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".functions.MakePackLot"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            tools:replace="android:screenOrientation" />
        <activity
            android:name=".functions.GetUserDetails"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".functions.AddUser"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".mapUsers.MapActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".Dashboard"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".oldCode.AllFunctions"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".oldCode.callFunction"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".AllContracts"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>